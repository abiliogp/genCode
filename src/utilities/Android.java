package utilities;

public class Android {

	public enum Classes {
		Activity, ListActivity, MapActivity, R, Thread;
	}
	
	public enum Methods{
		onCreate, onStop, onPause, onResume, onDestroy, onStart, onClick;
	}
	
	
	public enum Widget{
		ListView, EditText, Button, TextView, ProgressBar, LinearLayout, ImageView, ImageButton;
	}
	
	public enum View{
		Menu, SubMenu, MenuItem, MenuInflater, Window;
	}
	
	public enum Content{
		Intent;
	}
	
	public enum os{
		Bundle;
	}
}
