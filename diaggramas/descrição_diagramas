nome do arquivo: locadora
tem um diagrama de sequencia que representa o "opt" um if, sem o else

#dentro da classe cliente

if(x==1){
   veiculo.reservado();
}

locadora1
diagrama de sequencia que representa o "loop" um for

#dentro da classe cliente

for(i=1;i<=5;i++){
   veiculo.reservado();
}

Locadora2
diagrama de sequencia que representa o "alt" nesse caso pode ser um switch pois tem 3 operandos, quando tem dois operando pode ser um if-else

#dentro da classe cliente

switch(x){
caso 1:
    veiculo.reservado();
caso 2:
    veiculo.reservado();
caso 3:
    veiculo.reservado();
}



